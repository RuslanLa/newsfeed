type userType = {
  name: string,
  messagesCount: int,
  followersCount: int,
  followsCount: int,
  avatar: string
};
/* let user_of_get_posts_response = response =>
   switch response {
   | _ => {name: "", messagesCount: 0, followersCount: 0, followsCount: 0}
   }; */